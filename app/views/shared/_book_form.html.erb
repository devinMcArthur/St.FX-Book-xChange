<h4>Add a Textbook</h4>
<%= form_for(@book) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :title, placeholder: " Book Title: Course name" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
