<%= form_for(@book) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :title, placeholder: " Add a textbook..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
